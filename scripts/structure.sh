#!/usr/bin/env bash

mkdir -p cmd && touch $PWD/cmd/.keep
mkdir -p internal && touch $PWD/internal/.keep
mkdir -p pkg && touch $PWD/pkg/.keep
mkdir -p scripts && touch $PWD/scripts/.keep
mkdir -p api && touch $PWD/api/.keep
mkdir -p test && touch $PWD/test/.keep
